{
  "agents": [
    {
      "name"    : "<%= @db_name %>",
      "host"    : "<%= @db_host %>",
      "metrics" : "status,newrelic",
      "user"    : "<%= @db_user %>",
      "passwd"  : "<%= @db_pass %>"
    }
  ]
}